[
  {
    "id": 11,
    "title": "Create order from cart (POST /orders)",
    "epic": "Epic 1 — Order Creation",
    "labels": [
      "feature",
      "backend",
      "priority: high"
    ],
    "body": {
      "context": "When a user selects ticket types, a new `orders` record must be created in `pending` status and linked to one or more `order_items`.",
      "acceptance_criteria": [
        "Authenticated user can POST with `ticket_type_id` + `quantity`",
        "Server validates `quantity_sold < quantity_total` on `ticket_types`",
        "Creates `orders` row with `status = pending` and correct `total_amount`",
        "Creates one `order_items` row per ticket type with `unit_price` snapshot",
        "Returns `order.id` and full order payload",
        "Returns 409 if tickets are sold out"
      ]
    }
  },
  {
    "id": 12,
    "title": "Order expiry job — auto-cancel stale pending orders",
    "epic": "Epic 1 — Order Creation",
    "labels": [
      "backend",
      "priority: medium"
    ],
    "body": {
      "context": "Orders left in `pending` status must expire after a configurable TTL (e.g. 15 min) to free reserved seats.",
      "acceptance_criteria": [
        "Cron/worker runs every N minutes",
        "Sets `orders.status = expired` for unpaid orders past TTL",
        "Does not affect orders already in `paid` or `cancelled`",
        "Emits an event/log entry per expired order"
      ]
    }
  },
  {
    "id": 13,
    "title": "GET /orders/:id — order detail endpoint",
    "epic": "Epic 1 — Order Creation",
    "labels": [
      "api",
      "priority: medium"
    ],
    "body": {
      "context": "Provides a read endpoint for order details including nested items and current status.",
      "acceptance_criteria": [
        "Returns order with nested `order_items` and current `status`",
        "Only the owning user or an admin can fetch",
        "404 if not found, 403 if not owner"
      ]
    }
  },
  {
    "id": 14,
    "title": "Initiate payment — POST /payments",
    "epic": "Epic 2 — Payment Processing",
    "labels": [
      "feature",
      "backend",
      "priority: high"
    ],
    "body": {
      "context": "Creates a `payments` record linked to an `order_id` and calls the chosen payment provider to get a checkout URL / intent.",
      "acceptance_criteria": [
        "Accepts `order_id` and `provider` (enum)",
        "Validates order is still in `pending` status",
        "Creates `payments` row with `status = pending` and provider's `transaction_ref`",
        "Returns provider redirect URL or client secret",
        "Idempotent — re-calling returns the existing pending payment for the same order"
      ]
    }
  },
  {
    "id": 15,
    "title": "Payment webhook handler — confirm or fail payment",
    "epic": "Epic 2 — Payment Processing",
    "labels": [
      "feature",
      "backend",
      "priority: high"
    ],
    "body": {
      "context": "The payment provider calls back with success/failure. This handler must atomically update `payments` and `orders` and trigger ticket issuance on success.",
      "acceptance_criteria": [
        "Validates provider signature / HMAC before processing",
        "On success: sets `payments.status = paid`, sets `orders.status = paid`",
        "On failure: sets `payments.status = failed`, leaves order in `pending` (retriable)",
        "Publishes internal `payment.succeeded` event for ticket generation",
        "Returns HTTP 200 to provider regardless to prevent retries on already-processed events",
        "Idempotent — duplicate webhooks for same `transaction_ref` are no-ops"
      ]
    }
  },
  {
    "id": 16,
    "title": "Cancel order and refund — POST /orders/:id/cancel",
    "epic": "Epic 2 — Payment Processing",
    "labels": [
      "api",
      "priority: medium"
    ],
    "body": {
      "context": "Allows users or admins to cancel an order and trigger a refund if payment was already made.",
      "acceptance_criteria": [
        "Only callable by order owner or admin",
        "Sets `orders.status = cancelled`",
        "If a `paid` payment exists, triggers refund via provider and updates `payments.status`",
        "Invalidates any issued tickets for this order (`tickets.status = cancelled`)",
        "Returns 409 if order is already cancelled/expired"
      ]
    }
  },
  {
    "id": 17,
    "title": "Issue tickets on payment success",
    "epic": "Epic 3 — Ticket Issuance",
    "labels": [
      "feature",
      "backend",
      "priority: high"
    ],
    "body": {
      "context": "On `payment.succeeded` event, generate one `tickets` row per seat purchased, each with a unique QR code.",
      "acceptance_criteria": [
        "Listens to `payment.succeeded` event (or called synchronously from webhook handler)",
        "For each unit in `order_items.quantity`, creates a `tickets` row with `status = active`",
        "`ticket_code` is a UNIQUE, cryptographically random QR-safe string",
        "Sets `tickets.issued_at` to current timestamp",
        "Idempotent — re-running for the same `order_item_id` must not create duplicates"
      ]
    }
  },
  {
    "id": 18,
    "title": "Check-in endpoint — POST /event-checkins",
    "epic": "Epic 3 — Ticket Issuance",
    "labels": [
      "api",
      "priority: medium"
    ],
    "body": {
      "context": "Handles QR code scanning at event gates to mark attendees as checked in.",
      "acceptance_criteria": [
        "Accepts `ticket_code` (QR scan) and `gate`, `device_id`",
        "Validates ticket is `active` and belongs to the correct event",
        "Creates `event_checkins` row with `checked_in_at`",
        "Sets `tickets.status = used`",
        "Returns 409 if ticket already checked in"
      ]
    }
  },
  {
    "id": 19,
    "title": "DB migrations — orders, order_items, payments, tickets, event_checkins",
    "epic": "Epic 4 — Data Layer & Tests",
    "labels": [
      "database",
      "priority: high"
    ],
    "body": {
      "context": "Create all tables required by the order and payment cycle with correct schema, constraints, and indexes.",
      "acceptance_criteria": [
        "Migration creates all five tables with correct columns, types, and FK constraints per the ER diagram",
        "Indexes on `orders.user_id`, `order_items.order_id`, `payments.order_id`, `tickets.ticket_code` (unique), `tickets.order_item_id`",
        "Migration is reversible (down migration provided)"
      ]
    }
  },
  {
    "id": 20,
    "title": "Integration tests — full order-to-ticket happy path",
    "epic": "Epic 4 — Data Layer & Tests",
    "labels": [
      "testing",
      "priority: medium"
    ],
    "body": {
      "context": "End-to-end integration tests covering the full order, payment, and ticket issuance cycle including edge cases.",
      "scenarios": [
        "Create order → initiate payment → simulate webhook success → assert tickets created",
        "Create order → let it expire → assert status = expired, no tickets",
        "Create order → initiate payment → simulate webhook failure → assert order still pending, payment failed",
        "Duplicate webhook → assert idempotent (no double tickets)",
        "Check-in valid ticket → assert event_checkins row and ticket status = used",
        "Check-in already-used ticket → assert 409"
      ]
    }
  }
]